# 早上好 — 2026-06-26 验收清单

昨晚把手机端 + 网页端的**稳定性和 UI 细节**过了一遍(你选的两块:UI 打磨 + 稳定性/边界)。下面是全部改动 + 你该测什么。

## 一句话
**装 TestFlight 上的最新版(build number 11)**,前面的旧版都跳过。它包含今天一整天的所有修复 + 昨晚的优化。

---

## 昨晚这轮做了什么(优化 round 1)

### 🛡 稳定性 / 边界(给别人用也不崩)
- **加分支 AI 失败**:网络/AI 挂了不再是"未处理崩溃",保留本地即时推演。
- **AI 建议目标失败** → 返回空、显示「暂无建议」,不抛到界面。
- **对话卡死 bug**:聊天网络失败时,转圈以前会**永远卡住** → 现在保证停转 + 兜底回一句。
- **退化作息窗**(睡觉≤起床):两端共用的读取点 `dayWindow()` 统一兜底回默认窗,日视图不会再算出零/负高度的时间轴。引导那边也加了同样的拦截。(+1 个核心单测,网页 455 测试全过)
- **选时间滚轮**:窗口退化时也保证非空,不会空白/越界。
- (确认无虞:重置有二次确认弹窗;存档损坏会当作没有、不崩。)

### ✨ UI 细节打磨
- 按下反馈补齐:目标页领域胶囊 / AI 建议行、聊天快捷问句 / 发送键 —— 和首页/人生树/选时间统一。
- 无障碍标签:悬浮 + 号("添加任务")、选时间关闭("关闭")。

---

## 今天白天已做(也在这一版里)
- **人生树**:换成和**网页端一致的分支地图**(复用同一套布局几何)—— 从「现在」分叉的彩色曲线、自绘动画、光晕、左右滑看时间线、点曲线进聊天。
- **依赖根因修复**:之前"点不进人生树 / 选时间没反应"是 native 依赖版本和 SDK 不匹配(Expo Go 掩盖了,真机崩)。已全部对齐。
- **暖橙主题**:紫 → 暖橙,两端同色;领域色:事业蓝/财富绿/关系玫红/健康青/成长紫。
- **时间必填** + 自定义选时间滚轮;**短期目标**(目标下加阶段目标);**分步引导**(7 步 + 问专业);**作息原生时间弹窗**;**任务左滑删除**。

---

## 请重点测(build 11)
1. **人生树**:能进、曲线动画、左右滑、点曲线进"和未来的自己聊"。
2. **加任务**:加号 → 选时间(单独弹出)→ 添加 → 时间轴出现 → **左滑删除**。
3. **目标**:建目标、目标下加任务 / 加阶段目标、AI 建议目标。
4. **引导**(可在「我」→ 重置后重走):7 步、问专业、作息用原生时间弹窗。
5. 随便乱点 / 断网点 AI,看会不会崩或卡死(应该都有兜底)。

## 暂缓(等你决定)
- **登录 + 云端同步**:按你说的先不做。网页端的云同步代码是现成的(只差你建 Supabase 填两个值);手机端登录还没建。要上的时候叫我。

## 下一轮可继续优化的(还没做)
- 网页端用手机浏览器打开的响应式体验(你没选这块)。
- 手机端补齐网页端独有的:习惯打卡管理、数据统计/热力图、「我」页设置(作息/通知/主题)。
- 手机端更多视觉细节,需要你指一下哪儿还看着别扭。

测完把还不满意的点发我(最好截图),我接着改。
