# Archived project material

Files below this directory are retained for implementation history only. They are not the current source of truth.

Use `README.md`, `AGENTS.md`, `docs/README.md`, and the latest file in `docs/handoffs/` for current setup and project state.
