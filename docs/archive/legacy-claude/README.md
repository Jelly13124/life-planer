# Archived Claude configuration

This directory preserves the former Claude-specific project configuration. Current project instructions live in the root `AGENTS.md`, project skills in `.agents/skills/`, and Codex configuration in `.codex/`.
